services:
  frontend:
    ports:
      - 5173:5173
    env_file: ".env"
    healthcheck:
      test: curl -f http://localhost:5173/healthcheck || exit 1
      interval: 1m30s
      timeout: 30s
      retries: 5
      start_period: 30s
    build:
      context: .
      dockerfile: ./Dockerfile